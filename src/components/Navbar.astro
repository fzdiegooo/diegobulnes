<ul
  class="fixed top-0 right-0 justify-center items-end flex-col gap-4 pt-4 pr-4 hidden lg:flex"
>
  <li class="text-white text-sm font-semibold">
    <a href="/">Inicio</a>
  </li>
  <li class="text-white text-sm font-semibold">
    <a href="/about">Sobre mi</a>
  </li>
  <li class="text-white text-sm font-semibold">
    <a href="/projects">Proyectos</a>
  </li>
  <li class="text-white text-sm font-semibold">
    <a href="/contact">Contacto</a>
  </li>
</ul>
<ul
  class="fixed top-0 right-0 pt-4 pr-4 block lg:hidden"
>
  <svg 
  id="menuIcon"
  class="w-6 h-6" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"
    ><g id="SVGRepo_bgCarrier" stroke-width="0"></g><g
      id="SVGRepo_tracerCarrier"
      stroke-linecap="round"
      stroke-linejoin="round"></g><g id="SVGRepo_iconCarrier">
      <path
        d="M4 6H20M4 12H20M4 18H20"
        stroke="#ffffff"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"></path>
    </g></svg
  >

  <div id="menuBar" class="close absolute top-14 right-5 h-fit w-32 rounded-md">
    <ul class="w-full h-full flex flex-col justify-center items-center">
        <li class="text-white hover:bg-slate-800 w-full py-3 text-center">Inicio</li>
        <li class="text-white hover:bg-slate-800 w-full py-3 text-center">Tecnologias</li>
        <li class="text-white hover:bg-slate-800 w-full py-3 text-center">Proyectos</li>
        <li class="text-white hover:bg-slate-800 w-full py-3 text-center">Contacto</li>
    </ul>
  </div>
</ul>


<style>
  header {
    border-radius: 10px;
    border: 1px solid #32324d;
    background: #21213493;
    box-shadow: 0 4px 12px 0 rgba(41, 40, 117, 0.08);
  }

  #menuBar{
    background: rgba( 53, 53, 82, 0.8 );
    box-shadow: 0 8px 32px 0 rgba( 31, 38, 135, 0.37 );
    backdrop-filter: blur( 4.5px );
    -webkit-backdrop-filter: blur( 4.5px );
    border-radius: 10px;
    border: 1px solid rgba( 255, 255, 255, 0.18 );
  }

  .open{
    display: block;
  }

  .close{
    display: none;
  }
  
</style>

<script>
    let isOpen = false
    const menuIcon = document.getElementById("menuIcon")
    const menuBar = document.getElementById("menuBar")

    menuIcon?.addEventListener("click", () =>{
        isOpen = !isOpen

        if(isOpen){
            menuBar?.classList.add("open")
            menuBar?.classList.remove("close")
        }else{
            menuBar?.classList.add("close")
            menuBar?.classList.remove("open")
        }
    })

    
    
</script>
